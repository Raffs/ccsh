source ENV['GEM_SOURCE'] || 'https://rubygems.org'

# net-ssh
# net-ssh is the ruby library used to perform the ssh commands on ruby
gem "net-ssh", ">= 4.2.0"

# test tools
## the tools used for running the unit test
group(:test) do 
    gem "bundler", "~> 1.16"
    gem "rake", "~> 10.0"
    gem "rspec", "~> 3.0"
end